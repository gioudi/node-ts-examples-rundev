"use strict";
//Calculate power of a number
function calculatePower(base, exponent) {
    return Math.pow(base, exponent);
}
console.log(`2^3=${calculatePower(2, 3)}`);
console.log(`3^2=${calculatePower(3, 2)}`);
